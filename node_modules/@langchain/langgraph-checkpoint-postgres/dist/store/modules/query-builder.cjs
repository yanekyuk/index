
//#region src/store/modules/query-builder.ts
/**
* Shared query building logic for CRUD and search operations.
*/
var QueryBuilder = class {
	static buildFilterConditions(filter, params, paramIndex) {
		const conditions = [];
		let currentParamIndex = paramIndex;
		for (const [key, value] of Object.entries(filter)) if (typeof value === "object" && value !== null && !Array.isArray(value)) if (Object.keys(value).some((op) => op.startsWith("$"))) for (const [operator, operatorValue] of Object.entries(value)) {
			const result = this.buildOperatorCondition(key, operator, operatorValue, params, currentParamIndex);
			if (result.condition) {
				conditions.push(result.condition);
				currentParamIndex = result.newParamIndex;
			}
		}
		else {
			conditions.push(`value @> $${currentParamIndex}::jsonb`);
			params.push(JSON.stringify({ [key]: value }));
			currentParamIndex += 1;
		}
		else {
			conditions.push(`value ->> $${currentParamIndex} = $${currentParamIndex + 1}`);
			params.push(key, String(value));
			currentParamIndex += 2;
		}
		return {
			conditions,
			newParamIndex: currentParamIndex
		};
	}
	static buildOperatorCondition(key, operator, operatorValue, params, paramIndex) {
		switch (operator) {
			case "$eq":
				params.push(key, String(operatorValue));
				return {
					condition: `value ->> $${paramIndex} = $${paramIndex + 1}`,
					newParamIndex: paramIndex + 2
				};
			case "$ne":
				params.push(key, String(operatorValue));
				return {
					condition: `value ->> $${paramIndex} != $${paramIndex + 1}`,
					newParamIndex: paramIndex + 2
				};
			case "$gt":
				params.push(key, operatorValue);
				return {
					condition: `(value ->> $${paramIndex})::numeric > $${paramIndex + 1}`,
					newParamIndex: paramIndex + 2
				};
			case "$gte":
				params.push(key, operatorValue);
				return {
					condition: `(value ->> $${paramIndex})::numeric >= $${paramIndex + 1}`,
					newParamIndex: paramIndex + 2
				};
			case "$lt":
				params.push(key, operatorValue);
				return {
					condition: `(value ->> $${paramIndex})::numeric < $${paramIndex + 1}`,
					newParamIndex: paramIndex + 2
				};
			case "$lte":
				params.push(key, operatorValue);
				return {
					condition: `(value ->> $${paramIndex})::numeric <= $${paramIndex + 1}`,
					newParamIndex: paramIndex + 2
				};
			case "$in":
				if (Array.isArray(operatorValue) && operatorValue.length > 0) {
					const placeholders = operatorValue.map((_, i) => `$${paramIndex + 1 + i}`);
					params.push(key, ...operatorValue.map(String));
					return {
						condition: `value ->> $${paramIndex} = ANY(ARRAY[${placeholders.join(",")}])`,
						newParamIndex: paramIndex + 1 + operatorValue.length
					};
				}
				break;
			case "$nin":
				if (Array.isArray(operatorValue) && operatorValue.length > 0) {
					const placeholders = operatorValue.map((_, i) => `$${paramIndex + 1 + i}`);
					params.push(key, ...operatorValue.map(String));
					return {
						condition: `value ->> $${paramIndex} != ALL(ARRAY[${placeholders.join(",")}])`,
						newParamIndex: paramIndex + 1 + operatorValue.length
					};
				}
				break;
			case "$exists":
				params.push(key);
				return {
					condition: operatorValue ? `value ? $${paramIndex}` : `NOT (value ? $${paramIndex})`,
					newParamIndex: paramIndex + 1
				};
			default: break;
		}
		return { newParamIndex: paramIndex };
	}
};

//#endregion
exports.QueryBuilder = QueryBuilder;
//# sourceMappingURL=query-builder.cjs.map