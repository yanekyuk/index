{
  "name": "@indexnetwork/edge-claw",
  "version": "0.2.1",
  "description": "On-device personal broker for the Index Network — installer + workspace bundle for OpenClaw.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "edge-claw": "./install.ts"
  },
  "files": [
    "install.ts",
    "reset.ts",
    "workspace/**/*.md",
    "README.md"
  ],
  "engines": {
    "bun": ">=1.0.0"
  }
}
