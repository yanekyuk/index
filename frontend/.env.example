# Protocol server URL (server-side only, used by Next.js rewrites to proxy /api/* requests)
# Default: http://localhost:3001 (no need to set in local dev)
# PROTOCOL_URL=http://localhost:3001

# BUTTONDOWN_API_KEY=